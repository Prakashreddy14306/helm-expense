apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}
  namespace: expense
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      project: expense
      tier: db # has to match .spec.template.metadata.labels
  serviceName: "{{ .Release.Name }}-headless"
  replicas: {{ .Values.statefulset.replicas }} # by default is 1
  minReadySeconds: 10 # by default is 0
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        project: expense
        tier: db # has to match .spec.selector.matchLabels
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: {{ .Release.Name }}
        image: joindevops/{{ .Release.Name }}:{{ .Values.statefulset.version }}
        volumeMounts:
        - name: {{ .Release.Name }}
          mountPath: /var/lib/{{ .Release.Name }}
  # Instead of creating a PVC resource, you can directly provide in statefulset
  volumeClaimTemplates:
  - metadata:
      name: {{ .Release.Name }}
      namespace: expense
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: "ebs-sc"
      resources:
        requests:
          storage: {{ .Values.statefulset.storage }}